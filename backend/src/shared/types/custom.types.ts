export enum UserRole {
	CUSTOMER = "customer",
	SELLER = "seller",
	ADMIN = "admin",
}

export enum SalesChannel {
	ONLINE = "online",
	INSTORE = "in_store",
}

export enum OrderStatus {
	PENDING = "pending",
	PROCESSING = "processing",
	COMPLETED = "completed",
	CANCELED = "canceled",
}
